/*import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Test;
import static org.junit.Assert.*;

import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.TimeUnit;
import java.util.Date;
import java.io.File;
import org.openqa.selenium.support.ui.Select;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.*;
import static org.openqa.selenium.OutputType.*;


public class TutorPage extends BaseTest {

    private By memember=By.className("member");
    private By namemember=By.className("name");
    private By positionmember=By.className("position");

    /* public  class Member {
        задать две переменные стринг для позиции и нейма
    }*/

  /*  @Test
    public void NewSP() {
        wd.get("http://skillsup.ua/about/our-team/");
        List<WebElement> members = wd.findElements(memember);
        for (WebElement member : members) {
            System.out.println(member.findElement(namemember).getText());
            System.out.println(member.findElement(positionmember).getText()); //здесь вызывать кклас и лодить в него переменные
        }
    }

*/
